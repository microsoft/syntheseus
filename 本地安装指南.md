# 📥 在本地使用Syntheseus多步逆合成教程

## 🎯 下载教程到本地的3种方法

---

## 方法1：下载压缩包（推荐）⭐

### 步骤：

1. **在Cursor中找到文件**：`syntheseus-tutorials.tar.gz`（在根目录）
2. **右键点击** → 选择 **"下载"** (Download)
3. **在本地解压**：
   ```bash
   # Mac/Linux
   tar -xzf syntheseus-tutorials.tar.gz
   
   # Windows: 使用 7-Zip、WinRAR 或 Windows 11 自带解压
   ```

### 解压后你会得到：
```
syntheseus-tutorials/
├── 多步逆合成教程-README.md
├── 简单示例.py
├── 教程文件索引.md
├── 如何在Cursor中查看教程.md
└── docs/
    ├── 多步逆合成使用指南.md
    └── tutorials/
        └── 多步逆合成完整教程.ipynb
```

---

## 方法2：逐个文件下载

在Cursor文件浏览器中：

1. 找到文件（如 `多步逆合成教程-README.md`）
2. **右键** → **下载**
3. 重复以下文件：
   - `多步逆合成教程-README.md`
   - `简单示例.py`
   - `docs/多步逆合成使用指南.md`
   - `docs/tutorials/多步逆合成完整教程.ipynb`

---

## 方法3：复制粘贴内容

### 步骤1：在本地创建文件夹

```bash
# 在你的本地电脑上
mkdir syntheseus-tutorials
cd syntheseus-tutorials
mkdir -p docs/tutorials
```

### 步骤2：创建文件并复制内容

我会在下面提供每个文件的完整内容，你可以：
1. 在本地创建对应的文件
2. 复制粘贴内容
3. 保存

（详见下方各文件内容）

---

## 🚀 在本地运行教程

### 前置要求：

1. **安装Python** (3.8+)
   ```bash
   python --version  # 检查版本
   ```

2. **安装Syntheseus**
   ```bash
   # 完整安装（推荐）
   git clone https://github.com/microsoft/syntheseus.git
   cd syntheseus
   conda env create -f environment_full.yml
   conda activate syntheseus-full
   pip install "syntheseus[all]"
   
   # 或者最小安装
   pip install syntheseus
   ```

### 运行示例：

```bash
# 进入教程目录
cd syntheseus-tutorials

# 运行Python示例
python 简单示例.py

# 或打开Jupyter Notebook
jupyter notebook docs/tutorials/多步逆合成完整教程.ipynb
```

---

## 📋 文件清单和用途

| 文件 | 用途 | 优先级 |
|------|------|--------|
| `多步逆合成教程-README.md` | 快速入门指南 | ⭐⭐⭐ 必读 |
| `简单示例.py` | 可运行的Python示例 | ⭐⭐⭐ 推荐运行 |
| `docs/多步逆合成使用指南.md` | 完整API文档 | ⭐⭐ 参考 |
| `docs/tutorials/多步逆合成完整教程.ipynb` | 交互式教程 | ⭐⭐⭐ 深入学习 |
| `教程文件索引.md` | 文件导航 | ⭐ 可选 |

---

## 🔧 本地环境配置

### 选项1：完整安装（推荐，支持所有模型）

```bash
# 克隆仓库
git clone https://github.com/microsoft/syntheseus.git
cd syntheseus

# 创建conda环境
conda env create -f environment_full.yml
conda activate syntheseus-full

# 安装所有依赖
pip install "syntheseus[all]"
```

### 选项2：最小安装（仅核心功能）

```bash
pip install syntheseus
```

### 选项3：自定义安装（选择特定模型）

```bash
# 只安装LocalRetro和可视化
pip install "syntheseus[local-retro,viz]"

# 可选模型：
# - chemformer
# - local-retro
# - megan
# - mhn-react
# - retro-knn
# - root-aligned
# - viz (可视化)
# - dev (开发工具)
```

---

## 💡 本地使用提示

### 1. 检查安装

```bash
python -c "import syntheseus; print(syntheseus.__version__)"
```

### 2. 运行快速测试

```bash
python << 'PYTHON'
from syntheseus import Molecule
from syntheseus.reaction_prediction.inference import LocalRetroModel

mol = Molecule("c1ccccc1")
model = LocalRetroModel()
print("✅ Syntheseus安装成功！")
PYTHON
```

### 3. 配置原料库

在本地创建 `building_blocks.txt`：
```
Cc1ccc(B(O)O)cc1
Cc1ccc(Br)cc1
Ic1ccccc1
B(O)(O)c1ccccc1
```

### 4. 运行搜索

```python
from syntheseus import Molecule
from syntheseus.reaction_prediction.inference import LocalRetroModel
from syntheseus.search.mol_inventory import SmilesListInventory
from syntheseus.search.algorithms.breadth_first import AndOr_BreadthFirstSearch

# 加载本地原料库
inventory = SmilesListInventory.load_from_file("building_blocks.txt")

# 初始化模型
model = LocalRetroModel(use_cache=True, default_num_results=50)

# 运行搜索
search = AndOr_BreadthFirstSearch(
    reaction_model=model,
    mol_inventory=inventory,
    limit_iterations=100,
    time_limit_s=60.0
)

target = Molecule("Cc1ccc(-c2ccc(C)cc2)cc1")
graph, _ = search.run_from_mol(target)
print(f"找到 {len(graph)} 个节点")
```

---

## 📚 本地学习路径

### 第一天：环境搭建
1. ✅ 安装Python和conda
2. ✅ 安装Syntheseus
3. ✅ 下载教程文件
4. ✅ 运行 `简单示例.py` 验证

### 第二天：基础学习
1. 📖 阅读 `多步逆合成教程-README.md`
2. 💻 运行并修改 `简单示例.py`
3. 📓 打开 Jupyter Notebook 教程

### 第三天：深入探索
1. 📚 阅读 `多步逆合成使用指南.md`
2. 🔬 尝试不同的模型和算法
3. 🎯 在自己的数据上测试

---

## ❓ 常见问题

### Q: 首次运行很慢？
A: 模型需要下载（~几百MB），会自动缓存到 `~/.cache/torch/syntheseus/`

### Q: 内存不足？
A: 使用较小的batch_size或只安装特定模型：
```bash
pip install "syntheseus[local-retro]"  # 只安装LocalRetro
```

### Q: GPU支持？
A: 确保安装了对应CUDA版本的PyTorch：
```bash
# 查看CUDA版本
nvidia-smi

# 安装对应的PyTorch
# 参考：https://pytorch.org/get-started/locally/
```

### Q: Windows上运行出错？
A: 某些模型可能需要WSL2或Linux环境，建议使用Docker或虚拟机

---

## 🔗 有用的链接

- 📖 [官方文档](https://microsoft.github.io/syntheseus/)
- 💻 [GitHub仓库](https://github.com/microsoft/syntheseus)
- 📝 [论文](https://pubs.rsc.org/en/content/articlelanding/2024/fd/d4fd00093e)
- 🐛 [问题反馈](https://github.com/microsoft/syntheseus/issues)

---

## 🎉 开始使用

下载完成后，直接运行：

```bash
cd syntheseus-tutorials
python 简单示例.py
```

**祝你在本地使用愉快！** 🚀
